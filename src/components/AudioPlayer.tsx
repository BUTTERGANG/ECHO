/**
 * Playback for a stored recording — NATIVE (iOS/Android).
 *
 * STUB. Native playback uses expo-audio against the file URI saved by
 * `audioStore.native.ts`; wired up in the native phase. Renders nothing for
 * now so the entry detail screen stays clean. Web resolves to
 * `AudioPlayer.web.tsx`.
 */
export function AudioPlayer(_props: { audioPath: string | null }) {
  return null;
}
