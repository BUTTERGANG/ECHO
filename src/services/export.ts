/**
 * Full data export — JSON + audio zip (handoff §8 Step 10, principle §2.4).
 * STUB: implemented in Phase 1 Step 10.
 */
export async function exportAllData(): Promise<string> {
  throw new Error('exportAllData() not implemented yet — see Phase 1 Step 10.');
}
